const country = "Finland";
const continent = "Europe";
let population = 5530000;
const language = "Finnish";

const description = " is in Europe, and its 5.530 million people speak Finnish";

let isIsland = "Finland";
isIsland = false;

console.log(isIsland);
console.log(country + "" + description);
console.log(continent);
console.log(population + 1, population / 2, population / 2);
console.log(language);

//Good practice is to use const when ever you can (immutable)
//Use let variable when you know it will change or need to change
